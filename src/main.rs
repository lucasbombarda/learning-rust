fn main() {
    println!("This is a repo for my studies in Rust.");
    println!("To run the code, pick a binary file and run it with cargo run --bin <binary_file>");
    println!("To run the code in sorts.rs, run cargo run --bin sorts");
}
